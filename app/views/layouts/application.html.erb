<!DOCTYPE html>
<html>
  <head>
    <title>AsYouCook</title>
    <meta charset="UTF-8">
    <!--レスポンシブの設定-->
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!--Fontawesome-->
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.15.1/css/all.css" integrity="sha384-vp86vTRFVJgpjF9jiIGPEEqYqlDwgyBgEF109VFjmqGmIY/Y4HV4d3Gp2irVfcrp" crossorigin="anonymous">

    <!--クロスサイトリクエストフォージェリ対策で記述-->
    <%= csrf_meta_tags %>
    <!--Content Scurity Policyを実装する記述-->
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
          <a class="navbar-brand p-3" href="/"><span>AsYouCook</span></a>
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon "></span>
          </button>

          <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav ml-auto">
            <% if user_signed_in? %>
              <li class="nav-item">
                <%= link_to mypage_path, class: "btn text-white" do %><i class="fas fa-home text-white"></i>マイページ<% end %>
              </li>
              <li class="nav-item">
                <%= link_to cooks_path, class: "btn text-white" do %><i class="fas fa-utensils"></i>みんなの料理<% end %>
              </li>

              <li class="nav-item">
                <%= link_to destroy_user_session_path,  class: "btn text-white", method: :delete do %><i class="fas fa-sign-out-alt text-white"></i>ログアウト<% end %>
              </li>
            <% else %>
              <li class="nav-item">
                <%= link_to new_user_registration_path, class: "btn text-white" do %><i class="fas fa-user-plus text-white"></i>新規登録<% end %>
              </li>
              <li class="nav-item">
                <%= link_to new_user_session_path, class: "btn text-white" do %><i class="fas fa-sign-in-alt text-white"></i>ログイン<% end %>
              </li>
            <% end %>
            </ul>
          </div>
        </div>
      </nav>
    </header>
    <main>
      <div class="container">
        <!--フラッシュメッセージ-->
          <%= render 'layouts/flash_messages' %>
        <!--end-->
        <div class="row justify-content-center my-5">
          <%= yield %>
        </div>
      </div>
    </main>
    <footer class="shadow-sm mt-5">
      <div class="container-fluid">
        <p class="text-muted text-center" style="font-weight: bold;"> AsYouCook.</p>
      </div>
    </footer>
  </body>
</html>
